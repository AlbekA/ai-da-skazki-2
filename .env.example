# Example env variables
# On your local machine (for dev) you can create a .env file (do NOT commit real keys to GitHub)
# For Netlify deployment, set these in Netlify UI: Site settings -> Build & deploy -> Environment -> Environment variables
GEMINI_API_KEY=YOUR_REAL_GENAI_API_KEY_HERE
